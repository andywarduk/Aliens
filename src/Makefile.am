bin_PROGRAMS = Aliens
Aliens_SOURCES = Aliens.cpp

# set the include path found by configure
AM_CPPFLAGS =  $(LIBSDL_CFLAGS) $(all_includes)

# the library search path.
Aliens_LDFLAGS = $(all_libraries) $(LIBSDL_RPATH)
Aliens_LDADD = $(LIBSDL_LIBS)
